{
  "name": "@photoshop-app/render",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "test": "vitest run --passWithNoTests",
    "bench": "npx tsx --tsconfig benchmarks/tsconfig.json benchmarks/render-benchmark.ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@photoshop-app/core": "workspace:*",
    "@photoshop-app/types": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
